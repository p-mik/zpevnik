# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
eggs/
parts/
var/
sdist/
develop-eggs/
.installed.cfg
lib/
lib64/
.venv/
.venv_tmp/

# Django
*.log
local_settings.py
db.sqlite3
media/
staticfiles/

# Environment
.env
.env.local
.env.*.local

# Docker
.docker/

# Frontend
frontend/node_modules/
frontend/dist/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Zdrojová/testovací PDF (cizí obsah, necommitovat) — bez lomítka na začátku
# platí v celém stromu, ne jen v kořeni repa. Kdyby někdy bylo potřeba
# commitnout PDF jako fixture, musí se přidat přes `git add -f`.
*.pdf

# Lokální override docker-compose (viz poznámka u web.volumes v README) —
# v produkci se /opt/zpevnik/media váže na skutečnou cestu na serveru, tady
# na Windows to samo o sobě nedrží obsah mezi restarty Docker Desktopu.
docker-compose.override.yml
